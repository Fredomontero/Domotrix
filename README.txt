To run the web application you just need to double click the index.html file, and to run the the script in python you just need to 
copy in the raspberry the app.py and the credential.json in the same folder, then run the script in the terminal with the command: python app.py
